---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

type Props = {
  src: ImageMetadata;
  alt: string;
  class?: string;
};

const props = Astro.props;
---

<div class={props.class}>
  <Image src={props.src} alt={props.alt} width={800} height={600} />
</div>
